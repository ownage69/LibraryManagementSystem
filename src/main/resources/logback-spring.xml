<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <springProperty
            scope="context"
            name="applicationName"
            source="spring.application.name"
            defaultValue="library-service"/>
    <springProperty
            scope="context"
            name="logPath"
            source="logging.file.path"
            defaultValue="logs"/>
    <springProperty
            scope="context"
            name="maxFileSize"
            source="logging.rotation.max-file-size"
            defaultValue="10KB"/>
    <springProperty
            scope="context"
            name="maxHistory"
            source="logging.rotation.max-history"
            defaultValue="14"/>
    <springProperty
            scope="context"
            name="totalSizeCap"
            source="logging.rotation.total-size-cap"
            defaultValue="1GB"/>

    <property
            name="logPattern"
            value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logPath}/${applicationName}.log</file>
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>
                ${logPath}/archive/${applicationName}.%d{yyyy-MM-dd}.%i.log.gz
            </fileNamePattern>
            <maxFileSize>${maxFileSize}</maxFileSize>
            <maxHistory>${maxHistory}</maxHistory>
            <totalSizeCap>${totalSizeCap}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <logger name="com.library" level="INFO"/>
    <logger name="com.library.aop" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.orm.jdbc.bind" level="OFF"/>
    <logger name="org.hibernate.stat" level="OFF"/>

    <springProfile name="loadtest">
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!loadtest">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>
</configuration>
